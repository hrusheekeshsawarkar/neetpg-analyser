# Large source PDFs (keep local only)
neet-pg-papers/

# Caches / tooling
.ruff_cache/
.opencode/
__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# Secrets
.env
.env.*
!.env.example
*.pem

# LLM run logs / checkpoints (may be large)
analysis/data/classify_llm_run.log
analysis/data/llm_classify_checkpoint.json

# Full-res extracted figures (manifest JSON is kept). Web gets a compressed
# JPEG subset under web/public/question_images/ (tracked for Vercel deploys).
analysis/data/question_images/

# Product / Next.js
web/node_modules/
web/.next/
web/.env.local
analysis/data/web_mirror/
