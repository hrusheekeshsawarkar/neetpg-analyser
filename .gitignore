# Large source PDFs (keep local only)
neet-pg-papers/

# Caches / tooling
.ruff_cache/
.opencode/
__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# Secrets
.env
.env.*
!.env.example
*.pem

# LLM run logs / checkpoints (may be large)
analysis/data/classify_llm_run.log
analysis/data/llm_classify_checkpoint.json

# Extracted question figure binaries (manifest JSON is kept)
analysis/data/question_images/

# Served copies under Next.js public (regenerated by seed --local-only)
web/public/question_images/

# Product / Next.js
web/node_modules/
web/.next/
web/.env.local
analysis/data/web_mirror/
